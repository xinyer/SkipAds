package com.skip.ads;import android.accessibilityservice.AccessibilityService;import android.view.accessibility.AccessibilityEvent;import android.view.accessibility.AccessibilityNodeInfo;import android.widget.Toast;import java.util.List;public class SkipAdsAccessibilityService extends AccessibilityService {    @Override    public void onAccessibilityEvent(AccessibilityEvent event) {        if (event == null) return;        if (event.getSource() == null) return;        int type = event.getEventType();        if (type == AccessibilityEvent.TYPE_WINDOW_STATE_CHANGED || type == AccessibilityEvent.TYPE_WINDOW_CONTENT_CHANGED) {            if (findTextPressIt(event.getSource(), "跳过")) {                performGlobalAction(AccessibilityService.GLOBAL_ACTION_BACK);                Toast.makeText(this, "跳过广告", Toast.LENGTH_SHORT).show();            }        }    }    @Override    public void onInterrupt() {        // Noop.    }    private boolean findTextPressIt(AccessibilityNodeInfo source, String text) {        if (source == null) {            return false;        }        List<AccessibilityNodeInfo> nodes = source.findAccessibilityNodeInfosByText(text);        if (nodes == null || nodes.isEmpty()) {            return false;        }        nodes.forEach(node -> {            node.performAction(AccessibilityNodeInfo.ACTION_CLICK);            node.recycle();        });        return true;    }}